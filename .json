{
  "rules": {
    "videos": {
      ".read": true,
      ".write": true
    },
    "favorites": {
      "$user_id": {
        ".read": "$user_id === auth.uid",
        ".write": "$user_id === auth.uid"
      }
    }
  }
}